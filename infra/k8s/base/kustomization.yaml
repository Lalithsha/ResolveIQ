apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: resolveiq
resources:
  - namespace.yaml
  - configmap.yaml
  - workloads.yaml
  - autoscaling.yaml
  - disruption-budgets.yaml
  - ingress.yaml
  - network-policies.yaml
labels:
  - pairs:
      app.kubernetes.io/part-of: resolveiq
    includeSelectors: true
